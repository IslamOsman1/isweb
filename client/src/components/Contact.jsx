import React, { useState, useRef } from 'react';
import { motion } from 'framer-motion';
import { Mail, Phone, Send, MapPin } from 'lucide-react';
import { useLanguage } from '../LanguageContext';
import { useSiteData } from '../data/DataContext';
function Contact() {
  const { t, lang } = useLanguage();
  const { content, addRequest } = useSiteData();
  const form = useRef();
  const [isSent, setIsSent] = useState(false);
  const [isSending, setIsSending] = useState(false);
  const sendEmail = async (e) => {
    e.preventDefault();
    setIsSending(true);
    const data = new FormData(form.current);
    await addRequest({ name: data.get('user_name'), email: data.get('user_email'), service: data.get('service_type'), message: data.get('message') });
    setIsSent(true); setIsSending(false); form.current.reset(); setTimeout(() => setIsSent(false), 4500);
  };
  return <section id="contact" className="py-24 relative z-10 bg-[#0b101e]"><div className="container mx-auto px-6 relative"><div className="bg-white/[0.03] border border-white/10 rounded-3xl p-8 md:p-14 backdrop-blur-md shadow-2xl"><div className="grid lg:grid-cols-2 gap-16"><div><h2 className="text-4xl md:text-5xl font-black mb-6">{t('contact.title1')} <span className="text-[#00b4db]">{t('contact.title2')}</span></h2><p className="text-gray-400 mb-10 leading-relaxed text-lg">{t('contact.desc')}</p><div className="space-y-6"><div className="flex items-center gap-4 group"><div className="w-14 h-14 bg-white/5 border border-white/10 rounded-2xl flex items-center justify-center group-hover:bg-[#00b4db]/20 transition"><Mail className="text-[#00b4db]" /></div><div><p className="text-sm text-gray-500 mb-1">{t('contact.email')}</p><p className="font-bold text-lg" dir="ltr">{content.settings.email}</p></div></div><div className="flex items-center gap-4 group"><div className="w-14 h-14 bg-white/5 border border-white/10 rounded-2xl flex items-center justify-center group-hover:bg-[#00b09b]/20 transition"><Phone className="text-[#00b09b]" /></div><div><p className="text-sm text-gray-500 mb-1">{t('contact.phone')}</p><p className="font-bold text-lg" dir="ltr">{content.settings.phone}</p></div></div><div className="flex items-center gap-4 group"><div className="w-14 h-14 bg-white/5 border border-white/10 rounded-2xl flex items-center justify-center group-hover:bg-[#00b4db]/20 transition"><MapPin className="text-[#00b4db]" /></div><div><p className="text-sm text-gray-500 mb-1">{t('contact.address')}</p><p className="font-bold text-lg">{lang === 'ar' ? content.settings.addressAr : content.settings.addressEn}</p></div></div></div><div className="flex flex-wrap gap-3 mt-8"><a href={content.settings.facebookUrl} target="_blank" rel="noreferrer" className="px-4 py-3 rounded-xl bg-white/5 border border-white/10 flex items-center gap-2 hover:text-[#00b4db] transition"><span className="font-black">f</span> Facebook</a><a href={content.settings.instagramUrl} target="_blank" rel="noreferrer" className="px-4 py-3 rounded-xl bg-white/5 border border-white/10 flex items-center gap-2 hover:text-[#00b4db] transition"><span className="font-black">◎</span> Instagram</a><a href={content.settings.githubUrl} target="_blank" rel="noreferrer" className="px-4 py-3 rounded-xl bg-white/5 border border-white/10 flex items-center gap-2 hover:text-[#00b4db] transition"><span className="font-black">⌘</span> GitHub</a></div></div><div className="bg-[#0b101e] border border-white/5 rounded-2xl p-8 shadow-inner relative">{isSent && <motion.div initial={{ opacity: 0, y: -10 }} animate={{ opacity: 1, y: 0 }} className="absolute -top-12 left-0 w-full bg-green-500/20 border border-green-500/50 text-green-400 p-3 rounded-xl text-center text-sm font-bold">{t('contact.success')}</motion.div>}<form ref={form} onSubmit={sendEmail} className="space-y-6"><div className="grid grid-cols-1 md:grid-cols-2 gap-6"><div><label className="block text-sm font-medium text-gray-400 mb-2">{t('contact.nameLabel')}</label><input type="text" name="user_name" required placeholder={t('contact.namePlace')} className="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-[#00b4db]" /></div><div><label className="block text-sm font-medium text-gray-400 mb-2">{t('contact.emailLabel')}</label><input type="email" name="user_email" required placeholder={t('contact.emailPlace')} className="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-[#00b4db]" dir="ltr" /></div></div><div><label className="block text-sm font-medium text-gray-400 mb-2">{t('contact.serviceLabel')}</label><select name="service_type" className="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-[#00b4db] appearance-none" dir={lang === 'ar' ? 'rtl' : 'ltr'}><option className="bg-[#0b101e]">{t('contact.opt1')}</option><option className="bg-[#0b101e]">{t('contact.opt2')}</option><option className="bg-[#0b101e]">{t('contact.opt3')}</option><option className="bg-[#0b101e]">{t('contact.opt4')}</option></select></div><div><label className="block text-sm font-medium text-gray-400 mb-2">{t('contact.msgLabel')}</label><textarea name="message" required rows="4" placeholder={t('contact.msgPlace')} className="w-full bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-white focus:outline-none focus:border-[#00b4db] resize-none" /></div><button type="submit" disabled={isSending} className={`w-full text-white font-bold py-4 rounded-xl transition flex justify-center items-center gap-2 ${isSending ? 'bg-gray-600 cursor-not-allowed' : 'bg-gradient-to-r from-[#00b4db] to-[#00b09b] hover:opacity-90'}`}>{isSending ? t('contact.sending') : t('contact.send')}{!isSending && <Send size={18} className={lang === 'ar' ? 'rotate-180' : ''} />}</button></form></div></div></div></div></section>;
}
export default Contact;
